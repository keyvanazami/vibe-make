# Python dependencies for vibe-make's STEP (B-rep) export pipeline.
#   pip install -r requirements.txt
# Provides the OpenCASCADE bindings (OCP) used by scripts/stl_to_step.py to
# convert the OpenSCAD mesh into a STEP solid for Fusion 360 / CAD tools.
cadquery-ocp==7.9.3.1
